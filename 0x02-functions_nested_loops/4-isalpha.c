#include "main.h"

/**
 * _isalpha - checks for alphabetical letters
 * @c: a character to be check on
 * Return:return 0 or 1 depending on conditioning
 */
int _isalpha(int c)
{
	return ((c >= 'a' && c <= 'z') || (c >= 'A' && c ,= 'Z'))
}		
